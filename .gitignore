# 动态生成文件（diy.sh after 阶段生成，不提交；FILES 源为 files/fanchmwrt，仓库根 files/etc 恒为构建期副本）
files/etc/uci-defaults/
files/etc/shadow
files/etc/firstboot-pkgs/

# 动态下载文件（构建时下载，不提交）
files/etc/openclash/core/
files/usr/bin/AdGuardHome

# 离线 .apk 包：需随固件打包（首启经 firstboot-pkgs 用 --allow-untrusted 安装），
# 故纳入版本库、不忽略；kmod 类须与自编译内核 vermagic 一致。

# 构建输出
bin/
build_dir/
staging_dir/
tmp/
log/
workflow_*.json

# 编辑器
.workbuddy/
.vscode/
.idea/
*.swp
*.swo

